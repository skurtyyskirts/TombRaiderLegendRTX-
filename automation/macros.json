{
  "skip_cutscene": {
    "description": "Skip in-game cutscene (ESC -> W -> ENTER after 3s)",
    "steps": "WAIT:3000 ESCAPE WAIT:500 W WAIT:500 RETURN"
  },
  "test_session": {
    "description": "Full test: skip cutscene then idle (no movement macro)",
    "steps": "WAIT:3000 ESCAPE WAIT:500 W WAIT:500 RETURN"
  }
}
